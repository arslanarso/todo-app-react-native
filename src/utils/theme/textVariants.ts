/**
 * Represents predefined text variants with associated font sizes and line heights.
 */
export const textVariants = {
  /**
   * Default text variant with no specific styling.
   */
  defaults: {},
  /**
   * Base text variant with a standard font size and line height.
   */
  textBase: {
    fontSize: 16,
    lineHeight: 24,
  },
  /**
   * Large text variant with an increased font size and line height.
   */
  textLg: {
    fontSize: 18,
    lineHeight: 28,
  },
  /**
   * Extra-large text variant with a larger font size and line height.
   */
  textXl: {
    fontSize: 20,
    lineHeight: 28,
  },
  /**
   * Double extra-large text variant with a very large font size and line height.
   */
  text2Xl: {
    fontSize: 24,
    lineHeight: 32,
  },
  /**
   * Quadruple extra-large text variant with an extremely large font size and line height.
   */
  text4Xl: {
    fontSize: 36,
    lineHeight: 40,
  },
};
